name: CI
on:
  push:
    branches: [main]
    paths:
      - 'Sources/**'
      - 'Tests/**'
      - 'Package.swift'
      - 'Package.resolved'
      - 'action.yml'
      - 'specs/**'
      - 'fledge.toml'
      - '.github/workflows/**'
      - '.attest.json'
      - 'INTENT.md'
      - 'hi/**'
  pull_request:
    branches: [main]
    paths:
      - 'Sources/**'
      - 'Tests/**'
      - 'Package.swift'
      - 'Package.resolved'
      - 'action.yml'
      - 'specs/**'
      - 'fledge.toml'
      - '.github/workflows/**'
      - '.attest.json'
      - 'INTENT.md'
      - 'hi/**'
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
jobs:
  # -----------------------------------------------------------------------
  # macOS: full pipeline — build, test, spec check, dogfood, notes push.
  # -----------------------------------------------------------------------
  build-test-macos:
    runs-on: macos-latest
    timeout-minutes: 30
    # contents: write lets the dogfood step push the growing provenance ledger
    # to refs/notes/attest on pushes to main (no-op on PRs -- see the step below).
    # statuses: write lets the step below post a GitHub commit status named
    # "attest" so provenance surfaces as a check on the commit and PR pages
    # (git notes themselves are not shown in the GitHub web UI).
    permissions:
      contents: write
      statuses: write
    steps:
      # fetch-depth: 0 so the PR head commit is present locally on pull_request
      # events (checkout defaults to a shallow merge ref). attest sign validates
      # the target commit exists, so the head SHA it attests must be fetched.
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Install fledge
        run: brew install corvidlabs/tap/fledge
      - run: swift build
      - run: swift test
      - run: fledge spec check

      # --- Dogfood: attest records signed provenance on its OWN CI commit. ---
      # After build + test pass, attest attests this very commit, then gates it
      # against the committed .attest.json policy. The .attest.json policy is
      # permissive (it only requires an attestation), so this verify is a REAL,
      # fatal gate that passes precisely because we just recorded an attestation.
      - name: Build attest (release)
        run: swift build -c release

      - name: Dogfood -- attest attests this CI commit
        env:
          # Use the same SHA the verify step and commit status use: the PR head
          # on a pull_request, the pushed SHA otherwise. Signing the merge SHA on
          # a PR would leave the verified head un-attested.
          ATTEST_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          set -euo pipefail
          ATTEST=".build/release/attest"
          # Fetch the existing ledger so the new attestation appends to it and the
          # later push is a fast-forward. A plain checkout does not fetch notes,
          # so without this the push is rejected as non-fast-forward.
          git fetch origin "+refs/notes/attest:refs/notes/attest" 2>/dev/null || true
          echo "attest is attesting its own CI commit $ATTEST_SHA"
          # Record an agent:ci attestation on the commit under test. Signing in
          # CI is optional (no key is provisioned) -- an unsigned attestation is a
          # valid record, which is the whole point of attest.
          "$ATTEST" sign \
            --commit "$ATTEST_SHA" \
            --reviewer "agent:ci" \
            --confidence 0.9 \
            --tests-passed \
            --verdict proceed \
            --note "CI: swift build + swift test + fledge spec check all green"

      - name: Dogfood -- verify the CI commit against .attest.json (fatal gate)
        id: verify
        env:
          # The PR head SHA on a pull_request event, the pushed SHA otherwise.
          # This is the SHA the commit status is attached to below.
          ATTEST_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          set -uo pipefail
          ATTEST=".build/release/attest"
          echo "== ledger for $ATTEST_SHA =="
          "$ATTEST" log --commit "$ATTEST_SHA" --color always
          echo "== verify against the committed policy (fatal) =="
          # The committed .attest.json is permissive enough that the attestation
          # we just recorded satisfies it. We capture the exit code so the next
          # step can publish a commit status, then re-raise it so a real policy
          # violation still fails the job.
          "$ATTEST" verify --commit "$ATTEST_SHA" --policy .attest.json --color always
          code=$?
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"
          exit "$code"

      # Surface provenance in the GitHub web UI. Git notes (refs/notes/attest)
      # are NOT shown in the GitHub UI, so we publish a commit status named
      # "attest" that appears as a check on the commit and PR pages. This runs
      # even when verify fails (so a failure is reported), and is best-effort so
      # a token hiccup can never redden CI on its own.
      - name: Post attest commit status
        if: always() && steps.verify.outcome != 'skipped'
        env:
          GH_TOKEN: ${{ github.token }}
          ATTEST_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
          VERIFY_CODE: ${{ steps.verify.outputs.exit_code }}
        run: |
          set -uo pipefail
          if [ "${VERIFY_CODE:-1}" = "0" ]; then
            STATE="success"
            DESC="provenance verified"
          else
            STATE="failure"
            DESC="policy not satisfied"
          fi
          echo "posting attest commit status: $STATE ($DESC) on $ATTEST_SHA"
          gh api -X POST "repos/${{ github.repository }}/statuses/$ATTEST_SHA" \
            -f state="$STATE" \
            -f context="attest" \
            -f description="$DESC" \
            -f target_url="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}" \
            || echo "status post skipped"

      # PREFERRED real dogfooding: push the notes ref so attest's repo
      # accumulates a provenance ledger over time. Guarded to push only on a
      # push to main (never on PRs, which run from forks/branches and lack the
      # write token reliably). The push is best-effort: a permissions or race
      # issue prints a notice instead of reddening CI, so the gate above remains
      # the only thing that can fail this job.
      - name: Dogfood -- push the provenance ledger (main only, best-effort)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          set -uo pipefail
          echo "pushing refs/notes/attest so the ledger travels with the repo"
          git push origin refs/notes/attest || echo "note push skipped (perms/race) -- ledger recorded locally for this run"

  # -----------------------------------------------------------------------
  # Linux: swift build + swift test only.
  # Brew, fledge, dogfood, and notes steps are macOS-only and stay above.
  # -----------------------------------------------------------------------
  build-test-linux:
    runs-on: ubuntu-latest
    container: swift:6.0
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      # The Swift container runs as root; mark the checkout safe so git
      # commands inside tests do not refuse to run in the directory.
      - name: Mark checkout as safe for git
        run: git config --global --add safe.directory '*'
      - run: swift build
      - run: swift test

  smoke-test-action:
    # Direct SDD evidence: REQ-attest-action-001, REQ-attest-action-002,
    # and REQ-attest-action-003 are exercised through the released composite wiring.
    # Dogfood the bundled composite action (`uses: ./`) end to end: install →
    # checksum → verify. The other jobs invoke attest via .build/release
    # directly, so action.yml's own wiring (binary install, sha verification,
    # injection-safe env handling, version resolution) had no CI coverage.
    #
    # Run on BOTH hosted runners: macOS exercises the universal asset (and the
    # source-build fallback), while ubuntu-latest exercises the prebuilt Linux
    # download path -- the one that shipped a dynamically-linked binary failing
    # at runtime with a missing libswiftCore.so. A macOS-only smoke can't catch
    # that. A policy violation can't fail this wiring check.
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0   # the action's range needs history
      - name: Validate the action contract
        run: bash Tests/action_contract_test.sh
      - name: Run the bundled action against this checkout
        id: attest
        uses: ./
        # A policy violation (or missing policy) must not fail this WIRING smoke
        # test; the assertion below checks the action installed attest and ran.
        continue-on-error: true
        with:
          range: HEAD~1..HEAD
      - name: Assert the action installed attest
        env:
          BINARY: ${{ steps.attest.outputs.binary }}
          OUTCOME: ${{ steps.attest.outcome }}
        run: |
          set -euo pipefail
          if [ "$OUTCOME" != "success" ]; then
            echo "::error::The bundled action completed with outcome: $OUTCOME"
            exit 1
          fi
          if [ -z "$BINARY" ] || [ ! -x "$BINARY" ]; then
            echo "::error::The action did not install a runnable attest — install wiring is broken."
            exit 1
          fi
          "$BINARY" --version
          echo "Action OK — binary=$BINARY"

  intent-check:
    # Structural gate on the intent record in INTENT.md + hi/. `hi check` catches
    # only structural breakage -- a duplicate id, a case with no parent, an id
    # colliding with a retired one, a malformed id, an undeclared family, a
    # criterion stranded outside every section -- never unfinished intent. Kept as
    # its own ubuntu job so compiling the Rust CLI never slows the macOS pipeline,
    # and so a broken intent file is reported distinctly from a broken build.
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v5
      - name: Install hi
        run: cargo install human-intent --version 0.5.0 --locked
      - name: Check the intent record for structural problems
        run: hi check

  guard-no-self-hosted:
    # Security tripwire: this repo is PUBLIC, so a self-hosted runner would let a
    # fork PR run untrusted code on our machines. Parse each job's runs-on and
    # fail if any resolves to self-hosted. Inspecting runs-on specifically (vs
    # grepping the file) means the check can't false-match prose or its own job.
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v5
      - name: Assert no workflow uses a self-hosted runner
        run: |
          set -euo pipefail
          python3 -c "import yaml" 2>/dev/null || pip install -q pyyaml
          python3 - <<'PY'
          import sys, glob, yaml
          bad = []
          for f in sorted(glob.glob(".github/workflows/*.yml") + glob.glob(".github/workflows/*.yaml")):
              try:
                  doc = yaml.safe_load(open(f)) or {}
              except Exception as e:
                  print(f"::warning::could not parse {f}: {e}")
                  continue
              for jname, job in (doc.get("jobs") or {}).items():
                  if not isinstance(job, dict):
                      continue
                  ro = job.get("runs-on")
                  if isinstance(ro, str):
                      labels = [ro]
                  elif isinstance(ro, list):
                      labels = [str(x) for x in ro]
                  elif isinstance(ro, dict):
                      lbls = ro.get("labels", [])
                      labels = [lbls] if isinstance(lbls, str) else [str(x) for x in lbls]
                  else:
                      labels = []
                  if any("self-hosted" in s.lower() for s in labels):
                      bad.append(f"{f}: job '{jname}' runs-on={ro!r}")
          if bad:
              print("::error::A workflow targets a self-hosted runner. Public repos must use GitHub-hosted runners only.")
              for b in bad:
                  print("  " + b)
              sys.exit(1)
          print("OK: no self-hosted runner targeting in any workflow.")
          PY
